# Pick one provider. Default is gemini.
LLM_PROVIDER=gemini

# Required for the chosen provider:
GEMINI_API_KEY=
# OPENAI_API_KEY=

# Optional model override (defaults: gemini-2.5-flash, gpt-4o-mini)
# MODEL=gemini-2.5-flash

# Server port (default 8765)
# PORT=8765
